name: dependent-jobs

on:
  workflow_dispatch:
jobs:
  job1:
    needs: job2
    runs-on: ubuntu-latest
    steps:
      - name: Executing Job P
        run: echo "job1 executed 11"
  job2:
    runs-on: ubuntu-latest
    steps:
      - name: Executing Job 2
        run: echo "job2 executed1"
  # job3:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Executing Job 3
  #       run: echo "job3 executed"    
